---
// CSS
import '../css/global.css';

import { MAIN_TITLE } from '../../../CONFIGURATION.js';
import { getCollection } from 'astro:content';
import Header from './Header.astro';

// PROPS

export type Props = {
  title?: string;
  description?: string;
};

const { props } = Astro;

const title = `${props.title?.length ? props.title + ' - ' : ''}${MAIN_TITLE}`;

const pages = await getCollection('pages');
---

<html lang="en">
  <!-- HEAD -->
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="algolia-site-verification" content="48D119076FE89D4C" />

    <title>{title}</title>
    <meta name="title" content={title} />
    <meta name="description" content={props.description} />
  </head>

  <!-- BODY -->
  <body class="dark bg-zinc-800 font-sans text-zinc-100">
    <!-- header -->
    <Header />

    <!-- main content -->
    <main class="mt-8 mb-36 px-4">
      <slot />
    </main>
  </body>
</html>
